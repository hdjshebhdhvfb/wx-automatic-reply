"""
全局配置文件
修改此文件即可调整程序行为，无需改动其他代码。
"""

# ============================================================
# 微信设置
# ============================================================

# 微信窗口搜索超时 (秒)
WECHAT_WINDOW_TIMEOUT = 10

# 消息读取方式 (True=启用, False=禁用)
USE_UIA_READER = False          # UIA 控件读取 — 直接读微信 UI 文本
USE_CLIPBOARD_READER = False    # 剪贴板模式 — 点击消息→Ctrl+C 复制
USE_OCR_FALLBACK = True         # OCR 模式 — 截图→OCR 识别

# 新消息检查间隔 (秒) — 建议 0.5~2，太短会增加 CPU 占用
POLL_INTERVAL = 1.0

# ============================================================
# AI 模型设置
# ============================================================

# Ollama 服务地址
OLLAMA_BASE_URL = 'http://localhost:11434/v1/'

# 模型名称 (运行 `ollama list` 查看可用模型)
# 推荐: deepseek-r1:7b / deepseek-r1:14b / qwen2.5:7b
MODEL_NAME = 'deepseek-r1:7b'

# AI 回复温度 (0=精确, 1=随机, 建议 0.6~0.8)
TEMPERATURE = 0.7

# AI 回复最大 token 数
MAX_TOKENS = 300

# 每个会话保留的历史消息轮数
MAX_HISTORY_ROUNDS = 15

# ============================================================
# 系统提示词 (可自定义 AI 回复风格)
# ============================================================

SYSTEM_PROMPT = (
    "你是一个微信自动回复助手，请严格遵守以下规则：\n"
    "1. 用非常自然、口语化的中文回复，就像真人在聊天\n"
    "2. 回复要简短，一般1-3句话即可，不要长篇大论\n"
    "3. 不要使用任何Markdown格式、代码块\n"
    '4. 不要说「我是AI」「我是助手」之类的话\n'
    "5. 根据对话上下文保持连贯性\n"
    "6. 如果对方只是打招呼，简单回应即可"
)

# ============================================================
# 自定义 Skill
# ============================================================

# Skill 文件夹路径（存放用户自定义的 skill 文件，AI 会自动读取并遵循）
SKILLS_DIR = 'skills'

# ============================================================
# 好友列表
# ============================================================

# 好友列表文件路径 (每行一个好友名称)
NAMES_FILE = 'names.txt'

# ============================================================
# 监听模式
# ============================================================

# 模式 1: "specific" — 只回复 names.txt 中列出的好友
# 模式 2: "all"     — 回复所有发来消息的好友 (⚠️ 谨慎使用)
LISTEN_MODE = 'specific'

# 回复延迟 (秒) — 模拟人类打字，设为 0 则立即回复
REPLY_DELAY = 0.5

# 消息合并延迟 (秒) — 收到第一条消息后等待 N 秒，若期间有后续消息则合并后一起发给 AI
MESSAGE_MERGE_DELAY = 5

# ============================================================
# WeFlow SSE 推送设置
# ============================================================

# 启用 WeFlow SSE 消息推送（替代屏幕截取/OCR 方案）
WEFLOW_SSE_ENABLED = True

# WeFlow HTTP API 基础地址
WEFLOW_BASE_URL = 'http://127.0.0.1:5031'

# API Access Token
WEFLOW_ACCESS_TOKEN = ''

# SSE 推送地址（接收实时消息推送）
WEFLOW_SSE_URL = f'{WEFLOW_BASE_URL}/api/v1/push/messages?access_token={WEFLOW_ACCESS_TOKEN}'
