# ============================================
# 核心依赖 (必装)
# ============================================
openai>=1.0.0              # Ollama API 调用
uiautomation>=2.0.0        # Windows UI 自动化
pyperclip>=1.8.0           # 剪贴板操作
pyautogui>=0.9.0           # 截图 + 键盘鼠标模拟

# ============================================
# Web 控制台依赖
# ============================================
fastapi>=0.104.0           # Web 框架
uvicorn>=0.24.0            # ASGI 服务器

# ============================================
# 安装命令 (一键)
# ============================================
# pip install -r requirements.txt
#
# 如果要启用 OCR 模式 (推荐安装):
# pip install easyocr
# 或备选:
# pip install paddleocr
