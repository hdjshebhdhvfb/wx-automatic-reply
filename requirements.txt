# ============================================
# 核心依赖 (必装)
# ============================================
openai>=1.0.0              # Ollama API 调用
uiautomation>=2.0.0        # Windows UI 自动化
pyperclip>=1.8.0           # 剪贴板操作
pyautogui>=0.9.0           # 截图 + 键盘鼠标模拟
Pillow>=10.0.0             # 图像处理 (截图 / OCR)
numpy>=1.24.0              # 数组计算 (OCR)

# ============================================
# OCR 依赖 (可选 — 安装后启用 OCR 模式)
# ============================================
# easyocr>=1.7.0            # 推荐: 轻量级OCR，中文识别好，无需额外框架

# 备选 PaddleOCR:
# paddleocr>=2.7.0          # 百度OCR，需配合 PaddlePaddle
# paddlepaddle>=2.6.0

# ============================================
# 安装命令 (一键)
# ============================================
# pip install -r requirements.txt
#
# 如果要启用 OCR 模式 (推荐安装):
# pip install easyocr
# 或备选:
# pip install paddleocr
